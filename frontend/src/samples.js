// Runtime image registry. Images are added only after a user import or a
// selection from the connected Local Service; no synthetic data is loaded.
export const samples=[];
export const getImg=s=>s.data;
